const ConfigValidator = require('../../lib/config/ConfigValidator');

describe('ConfigValidator', () => {

    let validator;

    beforeEach(() => {
        validator = new ConfigValidator();
    });

    it('should throw error when there are no adapters', () => {
        
    });

});
